allprojects {
    apply plugin: 'java'

    dependencies {
        implementation project(":xar-core")
    }
}
subprojects {
    repositories {
        mavenLocal()

        mavenCentral()
    }
    dependencies {
        implementation libraries."$project.name"
    }
}